using System;
using System.Collections;
using System.Net.Sockets;

namespace ChatServer
{
    internal class handleClinet
    {
        internal void startClient(TcpClient clientSocket, string dataFromClient, Hashtable clientsList)
        {
            throw new NotImplementedException();
        }
    }
}